{
  "name": "bot-barbearia-mz-oficial",
  "version": "2.0.0",
  "description": "Bot de WhatsApp usando a API Oficial da Meta",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm install && npx prisma generate && npx prisma db push",
    "db:push": "npx prisma db push"
  },
  "dependencies": {
    "@prisma/client": "^5.10.0",
    "axios": "^1.6.7",
    "date-fns": "^3.3.1",
    "dotenv": "^16.4.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "prisma": "^5.10.0"
  },
  "author": "Desenvolvedor",
  "license": "ISC"
}